# 02 — Identity Governance

Focus: Entra ID automation, PIM, Conditional Access, and RBAC automation.

Structure:

- `/bicep` — (Optional) Bicep references or helper modules if used for identity automation.
- `/terraform` — Terraform examples using `azuread` and `azurerm` providers.
- `/docs` — Role definitions, RBAC matrices, and Conditional Access evidence.