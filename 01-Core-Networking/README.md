# 01 — Core Networking

Focus: Hub-and-Spoke networking, centralized egress, Azure Firewall, and UDRs.

Structure:

- `/bicep` — Bicep modules and parameter files for Hub and Spokes.
- `/terraform` — Terraform reference implementation and `backend.tf` example.
- `/docs` — Decision log, topology diagrams, and test evidence.